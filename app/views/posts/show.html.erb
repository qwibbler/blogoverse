<div class="post fullpage text_box sketchy">
  <div class="headline">
    <h3><%= @post.title %> by <%= link_to @post.user.name, user_path(@post.user) %> </h3>
    <%= render partial: 'shared/comments', object: @post %>
  </div>
  <%= @post.text %>
</div>
<%= list_comments(@comments) %>
